# F1C200S tuning:
# Prefer libfaad2 over MPlayer's default ffmpeg AAC path for local MP4/AAC.
# This reduces CPU pressure on ARM926-class targets without changing
# global ALSA device selection.
afm=faad,ffmpeg

